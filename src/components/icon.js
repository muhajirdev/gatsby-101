import React from "react";

export default ({ source }) => (
  <div style={{ width: "1.25rem", backgroundColor: "", }}>
    <img src={source} style={{ marginBottom: 0, display: "block" }} />
  </div>
);
