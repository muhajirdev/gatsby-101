import React from "react";

export default ({ source }) => (
  <div style={{ width: "1.25rem", backgroundColor: "", }}>
    <img alt="favicon" src={source} style={{ marginBottom: 0, display: "block" }} />
  </div>
);
